name: GitHub Actions Pandoc Demo
on: [push]
jobs:
  convert_via_pandoc:
    runs-on: ubuntu-latest
    steps:
      - run: echo "FILE=example.md" >> $GITHUB_ENV
      - uses: docker://pandoc/latex:2.9
        with:
          args: --standalone --output=index.html ${{ env.FILE }}
